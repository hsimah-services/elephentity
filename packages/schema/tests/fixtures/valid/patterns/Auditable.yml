pattern: Auditable
description: Writes an audit record after every commit.
use:
  - Timestamps
triggers:
  audit:
    on: [create, update]
    phase: postCommit
    handler: true
readPolicies:
  owner:
    handler: true
interface: true
