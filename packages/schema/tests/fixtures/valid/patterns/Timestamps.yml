pattern: Timestamps
description: Creation and modification stamps.
fields:
  createdAt:
    type: datetime
    description: When the row was first written. Filled by the framework.
    managed: created
  updatedAt:
    type: datetime
    description: When the row was last written. Filled by the framework.
    managed: modified
